# -----------------------------------------------------------------------------
# Example Environment Variables for Web-Nexus Agent
#
# 1. Copy this file to a new file named .env in the same directory.
# 2. Fill in your actual credentials in the .env file.
#
# Do NOT commit the .env file to version control.
# -----------------------------------------------------------------------------

# --- Model Provider ---
# Choose the large language model provider. Options: "google", "openai", or "anthropic".
# The agent will use the corresponding API key and model name below.
LLM_PROVIDER="google"

# --- API Keys ---
# Get your Google AI Studio API key from: https://aistudio.google.com/app/apikey
GOOGLE_API_KEY="your-google-api-key-here"
# Get your OpenAI API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY="your-openai-api-key-here"
# If using OpenRouter (OpenAI-compatible), you can set this instead of OPENAI_API_KEY
# and point OPENAI_API_BASE to https://openrouter.ai/api/v1
OPENROUTER_API_KEY="your-openrouter-api-key-here"
# Get your Anthropic API key from: https://console.anthropic.com/keys
ANTHROPIC_API_KEY="your-anthropic-api-key-here"

# --- Services API Keys ---
DEEPGRAM_API_KEY="your-deepgram-api-key-here"

# --- Model Names ---
# You can change these to other compatible models, e.g., "gpt-4o" or "gemini-1.5-pro-latest"
GEMINI_MODEL_NAME="gemini-1.5-flash"
OPENAI_MODEL_NAME="gpt-4o-mini"
ANTHROPIC_MODEL_NAME="claude-3-5-sonnet-20240620"

# --- Agent Settings ---
# Set to "true" to see the agent's detailed thought process and tool usage.
# Useful for debugging. Defaults to "False".
AGENT_VERBOSE="False"

# --- Input Settings ---
# Set to "true" to enable voice input via microphone using Deepgram.
ENABLE_VOICE_INPUT="False"

# --- Email Credentials ---
# Replace with your actual email provider's details and your credentials.
#
# IMPORTANT FOR GMAIL USERS:
# You MUST use an "App Password", not your regular Google account password.
# How to create an App Password for Gmail: https://support.google.com/accounts/answer/185833
#
# Common Server Settings:
# Gmail: IMAP="imap.gmail.com", SMTP="smtp.gmail.com", PORT="465"
# Outlook: IMAP="imap-mail.outlook.com", SMTP="smtp.office365.com", PORT="587"

IMAP_SERVER="imap.example.com"
SMTP_SERVER="smtp.example.com"
SMTP_PORT="465" # Use 465 for SMTP_SSL (e.g., Gmail) or 587 for STARTTLS (e.g., Outlook)
EMAIL_USER="your-email@example.com"
EMAIL_PASSWORD="your-email-password" # For Gmail, this is your 16-digit App Password.

# --- OpenRouter (optional) ---
# To use OpenRouter, set LLM_PROVIDER="openai", set OPENAI_API_BASE as below,
# and provide OPENAI_API_KEY (or OPENROUTER_API_KEY).
# Model names follow provider/model notation, e.g., "openai/gpt-4o-mini" or "anthropic/claude-3.5-sonnet".
OPENAI_API_BASE="https://openrouter.ai/api/v1"
# Recommended by OpenRouter to improve request acceptance:
# Use your app/site so they can contact you if necessary.
OPENROUTER_SITE="http://127.0.0.1:5001"
OPENROUTER_APP="WebNexus Agent"